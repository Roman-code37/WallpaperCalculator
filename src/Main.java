public class Main {
    public static void main(String[] args) {
        int length = 5 * 2;
        int width = 3 * 2;
        float heigth = 2.5F;
        int roll = 1;
        int length2 = 10;
        int perimetr = (length) + (width);
        int sum = perimetr / roll;
        float quantity = length2 / heigth;
        float total = 1 + sum / quantity;
        System.out.println(total);

    }
}
